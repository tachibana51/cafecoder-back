{
protocol        = udp
port            = 4444
socket_type     = dgram
wait            = no
user            = nologin
server          = /usr/sbin/in.tftpd
server_args     = /home/akane/cafe/cafecoder-back/submits/
disable         = no
}
